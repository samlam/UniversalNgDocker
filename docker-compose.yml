version: "3"

services:
  web:
    # image: node-build-local
    build: 
      context: ./web
      dockerfile: Dockerfile
    volumes:
      - ./web:/app
    #command: ash -c "pwd && ls -la && npm install"
    command: nodemon ./dist/server.js --watch --inspect=0.0.0.0:5858
    ports:
      - "8000:4200"

